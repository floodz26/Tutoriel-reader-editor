# Tutoriel-reader-editor
An Android app that makes it easy to read and build tutorials, with images and text organized into modules and steps encoded in Base64 in an importable and exportable .JSON file. French version
